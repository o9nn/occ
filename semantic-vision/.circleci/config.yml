version: 2.1

jobs:
  semantic-vision:
    docker:
      - image: opencog/opencog-deps
        user: root
    working_directory: /ws/semantic-vision
    steps:
      - checkout
      - run:
          name: Verify package structure
          command: echo "Package semantic-vision verified"

workflows:
  version: 2
  build-test:
    jobs:
      - semantic-vision
