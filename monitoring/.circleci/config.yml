version: 2.1

jobs:
  monitoring:
    docker:
      - image: opencog/opencog-deps
        user: root
    working_directory: /ws/monitoring
    steps:
      - checkout
      - run:
          name: Verify package structure
          command: echo "Package monitoring verified"

workflows:
  version: 2
  build-test:
    jobs:
      - monitoring
