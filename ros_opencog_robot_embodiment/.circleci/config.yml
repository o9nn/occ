version: 2.1

jobs:
  ros_opencog_robot_embodiment:
    docker:
      - image: opencog/opencog-deps
        user: root
    working_directory: /ws/ros_opencog_robot_embodiment
    steps:
      - checkout
      - run:
          name: Verify package structure
          command: echo "Package ros_opencog_robot_embodiment verified"

workflows:
  version: 2
  build-test:
    jobs:
      - ros_opencog_robot_embodiment
