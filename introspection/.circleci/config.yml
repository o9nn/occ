version: 2.1

jobs:
  introspection:
    docker:
      - image: opencog/opencog-deps
        user: root
    working_directory: /ws/introspection
    steps:
      - checkout
      - run:
          name: Verify package structure
          command: echo "Package introspection verified"

workflows:
  version: 2
  build-test:
    jobs:
      - introspection
