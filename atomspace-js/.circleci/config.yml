version: 2.1

jobs:
  atomspace-js:
    docker:
      - image: opencog/opencog-deps
        user: root
    working_directory: /ws/atomspace-js
    steps:
      - checkout
      - run:
          name: Verify package structure
          command: echo "Package atomspace-js verified"

workflows:
  version: 2
  build-test:
    jobs:
      - atomspace-js
